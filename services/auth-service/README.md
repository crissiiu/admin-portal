# Auth Service

Owns authentication workflows: register, login, refresh token, logout, forgot password, reset password, email verification, and sessions.

It must not own candidate/employer profile data beyond authentication identity and session state.
